import type { Prisma } from '../../../../prisma-client';
export type UserAdminTeams = (Prisma.TeamGetPayload<{
    select: {
        id: true;
        name: true;
        logoUrl: true;
        credentials?: true;
        parent?: {
            select: {
                id: true;
                name: true;
                logoUrl: true;
                credentials: true;
            };
        };
    };
}> & {
    isUser?: boolean;
})[];
/** Get a user's team & orgs they are admins/owners of. Abstracted to a function to call in tRPC endpoint and SSR. */
declare const getUserAdminTeams: ({ userId, getUserInfo, getParentInfo, includeCredentials, }: {
    userId: number;
    getUserInfo?: boolean;
    getParentInfo?: boolean;
    includeCredentials?: boolean;
}) => Promise<UserAdminTeams>;
export default getUserAdminTeams;
//# sourceMappingURL=getUserAdminTeams.d.ts.map