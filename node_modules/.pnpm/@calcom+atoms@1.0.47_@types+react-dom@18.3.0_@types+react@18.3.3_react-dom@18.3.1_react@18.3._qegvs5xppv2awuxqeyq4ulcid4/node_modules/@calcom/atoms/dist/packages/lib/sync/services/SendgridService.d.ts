import type { SendgridNewContact } from "../../Sendgrid";
import Sendgrid from "../../Sendgrid";
import type { ConsoleUserInfoType, WebUserInfoType } from "../ISyncService";
import type ISyncService from "../ISyncService";
import SyncServiceCore from "../ISyncService";
export default class SendgridService extends SyncServiceCore implements ISyncService {
    protected service: Sendgrid;
    constructor();
    upsert: (user: WebUserInfoType | ConsoleUserInfoType) => Promise<SendgridNewContact>;
    console: {
        user: {
            upsert: (consoleUser: ConsoleUserInfoType) => Promise<SendgridNewContact>;
        };
    };
    web: {
        user: {
            upsert: (webUser: WebUserInfoType) => Promise<SendgridNewContact>;
            delete: (webUser: WebUserInfoType) => Promise<import("@sendgrid/mail").ClientResponse>;
        };
    };
}
//# sourceMappingURL=SendgridService.d.ts.map