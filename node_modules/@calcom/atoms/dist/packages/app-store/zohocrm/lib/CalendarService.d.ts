import type { Calendar, CalendarEvent, EventBusyDate, IntegrationCalendar, NewCalendarEventType, Person } from "@calcom/types/Calendar";
import type { CredentialPayload } from "@calcom/types/Credential";
export type ZohoToken = {
    scope: string;
    api_domain: string;
    expires_in: number;
    expiryDate: number;
    token_type: string;
    access_token: string;
    accountServer: string;
    refresh_token: string;
};
export type ZohoContact = {
    Email: string;
};
export default class ZohoCrmCalendarService implements Calendar {
    private integrationName;
    private auth;
    private log;
    private client_id;
    private client_secret;
    private accessToken;
    constructor(credential: CredentialPayload);
    private createContacts;
    private contactSearch;
    private getMeetingBody;
    private createZohoEvent;
    private updateMeeting;
    private deleteMeeting;
    private zohoCrmAuth;
    handleEventCreation(event: CalendarEvent, contacts: CalendarEvent["attendees"]): Promise<{
        uid: any;
        id: any;
        type: string;
        password: string;
        url: string;
        additionalInfo: {
            contacts: Person[];
            meetingEvent: any;
        };
    }>;
    createEvent(event: CalendarEvent): Promise<NewCalendarEventType>;
    updateEvent(uid: string, event: CalendarEvent): Promise<NewCalendarEventType>;
    deleteEvent(uid: string): Promise<void>;
    getAvailability(_dateFrom: string, _dateTo: string, _selectedCalendars: IntegrationCalendar[]): Promise<EventBusyDate[]>;
    listCalendars(_event?: CalendarEvent): Promise<IntegrationCalendar[]>;
}
//# sourceMappingURL=CalendarService.d.ts.map