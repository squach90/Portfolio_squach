import type { TrpcSessionUser } from "../../../trpc";
import type { TConfirmInputSchema } from "./confirm.schema";
import type { BookingsProcedureContext } from "./util";
type ConfirmOptions = {
    ctx: {
        user: NonNullable<TrpcSessionUser>;
    } & BookingsProcedureContext;
    input: TConfirmInputSchema;
};
export declare const confirmHandler: ({ ctx, input }: ConfirmOptions) => Promise<{
    message: string;
    status: "ACCEPTED" | "REJECTED";
} | null>;
export {};
//# sourceMappingURL=confirm.handler.d.ts.map