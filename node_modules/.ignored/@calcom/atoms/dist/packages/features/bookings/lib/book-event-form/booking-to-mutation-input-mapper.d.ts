import type { PublicEvent, BookingCreateBody, RecurringBookingCreateBody } from "../../types";
export type BookingOptions = {
    values: Record<string, unknown>;
    event: PublicEvent;
    date: string;
    duration: number | undefined | null;
    timeZone: string;
    language: string;
    rescheduleUid: string | undefined;
    username: string;
    metadata?: Record<string, string>;
    bookingUid?: string;
    seatReferenceUid?: string;
    hashedLink?: string | null;
    orgSlug?: string;
};
export declare const mapBookingToMutationInput: ({ values, event, date, duration, timeZone, language, rescheduleUid, username, metadata, bookingUid, seatReferenceUid, hashedLink, orgSlug, }: BookingOptions) => BookingCreateBody;
export declare const mapRecurringBookingToMutationInput: (booking: BookingOptions, recurringCount: number) => RecurringBookingCreateBody[];
//# sourceMappingURL=booking-to-mutation-input-mapper.d.ts.map